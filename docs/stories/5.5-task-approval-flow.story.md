# Story 5.5: Tela de Aprovação de Tasks + Assign por Setor

**Epic:** Produto Base com Listings por Canal (Epic 5)
**ID:** STORY-5.5
**Status:** Ready
**Complexity:** M (8 points)
**Time Estimate:** 2 days

---

## Story

Como gestor, quero ver todas as tasks geradas pelos agentes especialistas (Alex/Marina/Sunny/Tren/Viral/Premium),
aprovar ou rejeitar cada uma com contexto claro (qual listing, qual agente recomendou, por quê),
e ao aprovar definir para qual setor ou responsável a task vai, para que a equipe saiba exatamente o que fazer.

**Dependencies:** STORY-5.1, STORY-5.3

---

## Acceptance Criteria

1. Página `/marketplace/aprovar` lista todas as tasks com status `pending` agrupadas por produto/listing
2. Cada task mostra: qual agente especialista criou (ex: "🛒 Alex — Amazon"), listing de origem, prioridade, horas estimadas
3. Aprovar task individual com 1 clique + seletor de setor (Conteúdo / Ads / Cadastro / Marketplace / Relatórios) e responsável (opcional)
4. Rejeitar task com motivo opcional
5. Aprovação em lote: selecionar várias tasks do mesmo canal e aprovar todas de uma vez
6. Ao aprovar, task muda para `approved` + `assigned_to` setado no Supabase
7. Tasks aprovadas desaparecem da fila e aparecem no módulo de tasks da equipe
8. Badge no sidebar mostrando contagem de tasks pendentes de aprovação
9. Filtros: por marketplace, por prioridade, por produto

---

## Scope

### IN
- Página `/marketplace/aprovar` (nova, substitui /marketplace/tasks para o fluxo de aprovação)
- Integração com APIs existentes: `/api/orchestration/tasks/approve` e `/api/orchestration/tasks/assign`
- Badge de contagem no sidebar
- Agrupamento por produto/listing
- Assign por setor (frente) ou usuário da equipe

### OUT
- Notificação push/email para o responsável (P2)
- Histórico de aprovações (P2)
- Editar task antes de aprovar (P2)

---

## Implementation Tasks

- [ ] Criar `app/(dashboard)/marketplace/aprovar/page.tsx`
- [ ] Criar `app/api/orchestration/tasks/route.ts` — adicionar campo `listing_id` no GET
- [ ] Criar componente `TaskApprovalCard` (task + contexto de agente + ações)
- [ ] Criar componente `AssignModal` (seletor de setor + responsável)
- [ ] Atualizar sidebar com badge de pendentes
- [ ] Atualizar story File List

---

## File List

- `app/(dashboard)/marketplace/aprovar/page.tsx` (NEW)
- `app/components/marketplace/TaskApprovalCard.tsx` (NEW)
- `app/components/marketplace/AssignModal.tsx` (NEW)
- `app/components/layout/Sidebar.tsx` (MODIFIED — badge)

---

## Change Log

- 2026-03-05: Story criada por @dev (Dex) — Epic 5
